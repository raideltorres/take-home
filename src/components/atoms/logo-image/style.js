import styled from "styled-components";

const StyledLogoImage = styled.div`
  .th-logo-image {
    &__image {
      max-width: 100%;
      height: auto;
    }
  }
`;

export default StyledLogoImage;
