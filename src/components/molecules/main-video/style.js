import styled from "styled-components";

const StyledMainVideo = styled.div`
  flex-direction: column;
  gap: 16px;

  .th-main-video {
    &__text {
      text-align: justify;
    }
  }
`;

export default StyledMainVideo;
